import styled from 'styled-components';


const Sub = styled.span`font-size:500;
                        letter-spacing:0.8rem;
                        color:var(--primary);
                        `
export {Sub};