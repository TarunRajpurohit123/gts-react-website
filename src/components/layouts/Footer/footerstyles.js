import styled from "styled-components";

const FooterWarpper = styled.section`background:var(--primaryBackground);
                                     height:30rem;
                                     margin-top:var(----sTop);`
                            
const Logo = styled.section`color:white`;
const Container = styled.div`width:92%;
                             margin:auto`;

export {FooterWarpper,Logo,Container}